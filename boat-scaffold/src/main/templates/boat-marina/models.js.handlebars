{{>licenseInfo}}
/**
This file contains all the model definitions found in the OpenAPI.
*/

var models = {}
var enums = {}
{{#each models}}
    {{#with model}}
        {{^isEnum}}
            models["{{name}}"] = {{{modelJson}}};
        {{/isEnum}}
        {{#isEnum}}
        models["{{name}}"] = {{{modelJson}}};
        {{/isEnum}}
    {{/with}}
{{/each}}

