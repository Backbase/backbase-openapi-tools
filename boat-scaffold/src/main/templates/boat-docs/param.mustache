<div class="fake property">
    <span class="name">{{baseName}}</span>
    <div class="json-schema-view">
        <div class="primitive">
            <span class="type">{{dataTypeDisplayName}}</span>
            {{#required}}
                <span class="required">*</span>
            {{/required}}
            <span class="format">{{format}}</span> 
            
            {{#hasDefaultValue}}
                <span class="default">
                    default: {{defaultValue}}
                </span>
            {{/hasDefaultValue}}

            {{#pattern}}
                <span>pattern:</span> pattern: <span class="pattern">{{.}}</span>
            {{/pattern}}

            {{#minLength}}
                <span class="range minLength">minLength:{{.}}</span>
            {{/minLength}}
            {{#maxLength}}
                <span class="range maxLength">maxLength:{{.}}</span>
            {{/maxLength}}
            {{#exclusiveMaximum}}
                <span class="range">exclusive maximum {{.}}</span>
            {{/exclusiveMaximum}}
            {{#exclusiveMinimum}}
                <span class="range">exclusive minimum {{.}}</span>
            {{/exclusiveMinimum}}

            {{#isRange}}
                <span class="range">minimum: {{mininum}} maximum: {{maximum}}</span>
            {{/isRange}}
            
            <div class="inner description">{{description}}</div>

            {{#isEnum}}
                <div>Enum: {{#allowableValues}}
                    {{#enumVars}}
                        <code>{{name}}</code>
                    {{/enumVars}}
                {{/allowableValues}}
                </div>
            {{/isEnum}}
        </div>
        {{#hasExamples}}
            <div class="examples">
                <h3><b>Examples</b></h3>
                {{#examples}}
                    {{#example}}
                    <div class="inner json-formatter-string">{{value}}</div>
                    {{/example}}
                {{/examples}}         
            </div>
        {{/hasExamples}}
    </div>


</div>
