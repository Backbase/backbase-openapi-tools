<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.backbase.oss.boat.example</groupId>
        <artifactId>boat-bay</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>lint-petstore-non-breaking</artifactId>

    <name>BOAT :: Boat Bay :: Lint Non Breaking Spec</name>

    <description>
        Example project showing the linting of a specification via boat-bay
        Using a spec with non breaking changes in petstore
    </description>

    <packaging>pom</packaging>
    <build>
        <plugins>
            <plugin>
                <groupId>com.backbase.oss</groupId>
                <artifactId>boat-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>lint</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>lint</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${project.basedir}/src/main/resources/petstore-new-non-breaking.yaml</inputSpec>
                            <output>${project.basedir}/src/main/resources/output</output>
                            <sourceId>5</sourceId>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


</project>
