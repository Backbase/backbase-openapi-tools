openapi: '3.0.3'

info:
  title: title
  version: version

paths: {}

components:
  schemas:
    One:
      $ref: nested-with-files/one.yaml
    Three:
      type: object
      properties:
        id:
          type: integer
